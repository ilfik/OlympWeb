@model IEnumerable<OlympWeb.Models.Questions>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.test_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.question_text)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.test_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.question_text)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_question }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_question }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_question })
        </td>
    </tr>
}

</table>
